/* eslint-disable */
/**
 * This file use for append to service worker generated by CRA
 */
self.addEventListener('message', (event) => {
  if (!event.data) return

  switch (event.data) {
    case 'skipWaiting': self.skipWaiting(); break
    default: break
  }
})
